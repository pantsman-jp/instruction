# Git ことはじめ

## 目次
1. [これは何](#1-これは何)
2. [初期化からコミットまで](#2-初期化からコミットまで)  
    2.1. [作業フォルダを作る](#21-作業フォルダを作る)  
    2.2．[リポジトリを作る](#22-リポジトリを作る)  
    2.3．[ファイルを追加する](#23-ファイルを追加する)  
    2.4．[変更を記録する](#24-変更を記録するコミット)  
3. [cloneのやり方](#3-clone-のやり方)
4. [チーム開発](#4-チーム開発と-github)

## 1. これは何
Git のコマンドを解説します。

## 2. clone（クローン）から pull request（プルリクエスト）まで
### 2.1. クローンする
ターミナルで、デスクトップまで移動します。

```
PS C:\Users\ユーザー名\desktop>
```

このようになっていれば良いです。

そして、クローンしたいリポジトリの URL をコピーし、以下をターミナルで実行します。

```
git clone リポジトリのURL
```

そして、以下のコマンドを順に行い、作業フォルダを VSCode で開きます。

```
cd リポジトリ名
code .
```

作業しましょう。

## 2.2. ブランチを作る
ファイルの変更が済んだら、ブランチを作ってから、コミットをします。

新しいブランチを作るには、ターミナルで以下を実行します。

```
git checkout -b 新しいブランチ名
```

ブランチ名は作業内容が分かるようにつけましょう。

一応、今自分がどのブランチで作業しているのか確認してみましょう。

```
git branch
```

さっき作ったブランチにいれば、OK です。

## 2.3. 変更を記録する（コミット）
ファイルの変更が済んだら、コミットをしましょう。

コミットの前には変更したファイルをステージング（変更の記録を残すファイルを選ぶ作業）する必要があります。
以下を実行しましょう。

```
git add .
```

これはすべてのファイルを記録に残す、という意味です。もちろん、特定のファイルのみをステージングすることもできます。
以下のように。

```
git add README.md
```

では、コミットしてみましょう。

```
git commit -m "コミットメッセージ"
```

これでリポジトリに最初の変更履歴が記録されます。

コミットメッセージは簡潔でわかりやすいものが良いです。一言で変更内容がわかるようにしましょう。

## 2.4 プッシュ と プルリクエスト
コミットが済んだらプッシュしましょう。以下をターミナルで実行するだけです。

```
git push --set-upstream origin さっき作ったブランチ名
```

これで、プッシュは完了です。

次にプルリクエストをしましょう。GitHub のリポジトリページに行きましょう。

## 4. チーム開発と GitHub
これを参考にします。

- [うさぎでもわかるチームGitHub開発手順書](https://zenn.dev/taku_sid/articles/20250409_github_team)
- [初学者向け：GitとGitHubを使ったチーム開発ガイド ～ブランチ作成からプルリク、最終マージまで～](https://qiita.com/free-honda/items/f67236ea732eb7473c86)

端的に言えば、作業ごとにブランチを作り、コミット、プッシュし、プルリクエストを出すと言う流れです。

---
Copyright © 2025 pantsman